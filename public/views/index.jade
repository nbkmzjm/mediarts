
doctype html
html(lang="en")
	head
		title Jade Page
		meta(charset='UTF-8')
		link(rel="stylesheet" type="text/css" href="bower_components/bootstrap/dist/css/bootstrap.css")
		link(rel="stylesheet" type="text/css" href="css/style.css")


	body
		#container

		

			//- table#main
			//- 	- var test = "hello"
			//- 	p#test dateHeader 
			//- 	tr
			//- 		td= 'User'
			//- 		- for (var i=4; i < 14 ; i++)
			//- 			td= dateHeader[i].dateH
			//- 	- for (var i=0; i< users.length; i++)
			//- 		tr
			//- 			td= users[i].email
						//- - for (var e=0; e< 14; e++)
							if (typeof assigns[9] == "undefined")
								td= a
							else 

								assigns [e].datePos == e && assigns[e].user.email == users[i].email) 
								td= assigns[e].Note
						



			


	a(href='/assignInput') Assign Input
	br
	a(href='/logout') Logout

			


			
		script(src="public/js/jquery-2.1.4.min.js")
		script(src="public/js/socket.io-1.3.7.js")
		script(src="public/js/app.js")
		script.

			$(function onload(){
				var dateSC = new Date();
				var userslength = '#{users.length}';
				//- var usersemail = '#{users[0].email}';
				//- alert(usersemail);
				


				var container = $('#container');
				var table = document.createElement('table');
					table.setAttribute('id', 'main');
					var tr1 = document.createElement('tr');
						var td = document.createElement('td');
							td.appendChild(document.createTextNode('User'));
						tr1.appendChild(td);
						for (var i=0; i<7; i++){
							dateSC.setDate(dateSC.getDate()+1);
						var td = document.createElement('td');
							td.appendChild(document.createTextNode(dateSC.toDateString()));
							tr1.appendChild(td);
							}
					table.appendChild(tr1);
				for (var i=0; i< userslength; i++){
					var tr = document.createElement('tr');
						var td = document.createElement('td');
							td.appendChild(document.createTextNode('#{users[0].email}'));
						tr.appendChild(td);
					table.appendChild(tr);
				}
						
				
				container.append(table);

				//- $.post('/mainSC', { postdata:{name: "John", time: "2pm" }}).
				//- 	done(function(data){
				//- 	alert(data.dateHeader[0].dateH);
				//- 	alert(data.users[0].email);
				//- 	})
			});

